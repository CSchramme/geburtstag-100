import DisplayApp from '@/components/DisplayApp';

export const metadata = {
  title: 'Beamer – 100 Jahre',
  robots: { index: false, follow: false }
};

export default function DisplayPage() {
  return <DisplayApp />;
}
